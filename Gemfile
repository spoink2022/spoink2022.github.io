source "https://rubygems.org"

# Jekyll and the Minima theme
gem "jekyll", "~> 4.0"
gem "minima"

# Use GitHub Pages environment if deploying there (optional)
group :jekyll_plugins do
  gem "github-pages", "~> 227", :platforms => [:ruby]
end
